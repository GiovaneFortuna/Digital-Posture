<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 1. Permissões de Hardware -->
    <!-- Permite que o app abra a câmera -->
    <uses-permission android:name="android.permission.CAMERA" />
    
    <!-- Permite gravar áudio (Remova se não for usar vídeos com som para economizar memória) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- 2. Declaração de Recursos de Hardware -->
    <!-- Garante que o app só apareça na Play Store para dispositivos que tenham câmera -->
    <uses-feature android:name="android.hardware.camera" />
    <!-- O foco automático é importante para a análise da IA, mas colocamos como false 
         para não impedir a instalação em tablets simples que não possuem autofocus -->
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:label="digital_posture"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <!-- Tema que aparece enquanto o Flutter carrega -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
              
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Meta-data para a engine do Flutter -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Queries para compatibilidade com Android 11+ -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>